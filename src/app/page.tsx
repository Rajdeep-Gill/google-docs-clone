import { Button } from '@/components/ui/button';

const Home = () => {
  return (
    <div className='bg-blue-300 flex flex-col min-h-screen items-center justify-center text-8xl'>
      Hello World!
    </div>
  );
};

export default Home;
